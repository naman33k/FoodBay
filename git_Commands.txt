Git hub commands

to pull/checkout
1. git pull origin master
2. git fetch origin    // makes a diff branch (just to see what changes ve been done)
3. git merge origin/master master


to push
git push origin


remote alias 
git remote add "alias" url

commit
git commit -am "message"

add
git add file

log
git log
